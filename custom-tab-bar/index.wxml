<van-tabbar active="{{ active }}" bind:change="onChange">
  <van-tabbar-item wx:for="{{ list }}" wx:key="index" icon='{{ item.icon }}' size='24px'>
  {{ item.text }}
  </van-tabbar-item>
</van-tabbar>
